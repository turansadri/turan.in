# turan.in

`npm run dev` for development.

`npm run build` builds into www folder.

`python3 -m http-server 8000` to start a server (www folder).

Just `scp -r . root@turan.in:/var/www/turan.in` when ready